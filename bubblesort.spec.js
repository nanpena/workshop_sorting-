// describe('Bubble Sort', function(){

    
    
//     beforeAll(function () {
//         swap = jasmine.createSpy('testSwap',swap).and.callThrough();
//         // spyOn(swap).and.callThrough(); // replace existing `tootsiepop['lick']` method
//       });
//       it('call swap more than 0 but less than 100', function () {
//         bubbleSort([2,56,13,90,45]);
//         expect(swap.calls.count()).toBeGreaterThan(0);
//         expect(swap.calls.count()).toBeLessThan(100);
//       });

//     it('handles an empty array', function(){
//       expect( bubbleSort([]) ).toEqual( [] );
//     });

//     it('handles an array with single item', function(){
//         expect( bubbleSort([10]) ).toEqual( [10] );
//       });

    
//     it('handles an array with multiple items', function(){
//         expect( bubbleSort([2,4,1,7,5]) ).toEqual( [1,2,4,5,7] );
//       });

      

//   });

  

